import hardhat from "hardhat";

async function main() {
    console.log("deploy start")

    //TODO:
}

main()
    .then(() => process.exit(0))
    .catch(error => {
        console.error(error);
        process.exit(1);
    });
